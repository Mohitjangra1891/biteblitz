import 'package:biteblitz/src/utils/router.dart';import 'package:flutter/material.dart';import 'package:go_router/go_router.dart';import '../../../common/views/widgets/button.dart';import '../../../res/colors.dart';import '../../../res/imges.dart';import '../../../utils/SharedPrefHelper.dart';class pending_verification extends StatelessWidget {  const pending_verification({super.key});  @override  Widget build(BuildContext context) {    final screenHeight = MediaQuery.of(context).size.height;    final screenWidth = MediaQuery.of(context).size.width;    return SafeArea(      child: Scaffold(          body: Padding(        padding: EdgeInsets.all(screenHeight * 0.03),        child:            Column(mainAxisAlignment: MainAxisAlignment.end, crossAxisAlignment: CrossAxisAlignment.center, children: [          const Text(            'Thank you for submitting your documents!',            style: TextStyle(fontSize: 18, color: AppColors.primary_orange_color, fontWeight: FontWeight.w600),          ),          SizedBox(height: screenHeight * 0.01),          const Text(            'Your document upload was successful.',            style: TextStyle(fontSize: 14, color: AppColors.dark_text_color, fontWeight: FontWeight.w600),          ),              SizedBox(height: screenHeight * 0.02),              Padding(            padding: EdgeInsets.symmetric(horizontal: screenHeight * 0.02),            child: Image.asset(              AppImages.pending_verification_bg,              height: screenHeight * 0.25,            ),          ),          SizedBox(height: screenHeight * 0.02),          const Text(            'Your documents are currently being reviewed and verified.',            style: TextStyle(fontSize: 14, color: AppColors.dark_text_color, fontWeight: FontWeight.w600),          ),          SizedBox(height: screenHeight * 0.01),          const Text(            'You will receive confirmation via email once the verification process is complete.',            style: TextStyle(fontSize: 14, color: AppColors.primary_orange_color, fontWeight: FontWeight.w600),          ),              // Spacer(),              SizedBox(height: screenHeight * 0.12),              button_Primary(            title: "ok",            onPressed: () async {              // Navigator.pushReplacement(              //   context,              //   MaterialPageRoute(builder: (context) => dashboard()),              // );              await SharedPrefHelper.saveValue<int>(SharedPrefKeys.onboarding_stage, 10);              context.pushNamed(routeNames.dashboard);            },          ),        ]),      )),    );  }}